autowatch = 1;
outlets = 2;
var p = this.patcher;

// inlet = p.getnamed("inl1");


function packFromMessage(message) {
    post("lol")
    post(message)
    // var split = message.split(" ");
    // split.reverse();
    // split.pop();
    // split.reverse();
    // outlet(1, split)
}